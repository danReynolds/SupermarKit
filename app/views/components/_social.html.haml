.social-icons
  = link_to auth_at_provider_path(provider: :facebook), class: 'btn facebook' do
    .left #{button_label} Facebook
    = fa_icon 'facebook', class: 'right'
  = link_to auth_at_provider_path(provider: :google), class: 'btn google' do
    .left #{button_label} Google
    = fa_icon 'google', class: 'right'
  = link_to auth_at_provider_path(provider: :github), class: 'btn github' do
    .left #{button_label} GitHub
    = fa_icon 'github', class: 'right'

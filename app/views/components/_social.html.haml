.social-icons
  = link_to auth_at_provider_path(provider: :facebook), data: { turbolinks: false }, class: 'btn facebook' do
    .left #{button_label} Facebook
    = icon 'facebook', class: 'right'
  = link_to auth_at_provider_path(provider: :google), data: { turbolinks: false }, class: 'btn google' do
    .left #{button_label} Google
    = icon 'google', class: 'right'
  = link_to auth_at_provider_path(provider: :github), data: { turbolinks: false }, class: 'btn github' do
    .left #{button_label} GitHub
    = icon 'github', class: 'right'

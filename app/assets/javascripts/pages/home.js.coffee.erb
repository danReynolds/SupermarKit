$ ->
  onPage 'pages home', ->
    $nav = $('nav')

    $(window).scroll ->
      if ($(@).scrollTop() == 0)
        $nav.addClass('top')
      else
        $nav.removeClass('top')

    # Trigger a scroll to determine the nav color
    $(window).scroll()

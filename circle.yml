dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install libleptonica-dev libtesseract-dev
    - sudo mkdir /usr/local/share/tessdata
    - sudo curl -L https://github.com/tesseract-ocr/tessdata/raw/master/eng.traineddata > /usr/local/share/tessdata/eng.traineddata
